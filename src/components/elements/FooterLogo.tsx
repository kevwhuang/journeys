import React from 'react';

function FooterLogo(): React.ReactElement {
    return (
        <section className="footer__logo" hidden>
            <img src="" alt="" />
            <span title="journeys"></span>
        </section>
    );
}

export default FooterLogo;
