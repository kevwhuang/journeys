import React from 'react';

// import useZustand from '../../hooks/useZustand';

function ControlsSystemModal(): React.ReactElement {
    // const notifications = useZustand(s => s.records.notifications);

    return (
        <section className="controls__system--modal">
        </section>
    );
}

export default ControlsSystemModal;
