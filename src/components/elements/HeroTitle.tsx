import React from 'react';

function HeroTitle(): React.ReactElement {
    return (
        <section className="hero__title">
            <h1>Welcome, Adventurer.</h1>
        </section>
    );
}

export default HeroTitle;
