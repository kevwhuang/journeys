import React from 'react';

function ControlsNavbar(): React.ReactElement {
    return (
        <section className="controls__navbar" hidden>
            <i title="navbar"></i>
        </section>
    );
}

export default ControlsNavbar;
