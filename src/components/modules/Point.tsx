import React from 'react';

import '../../styles/modules/Point.scss';

function Point(): React.ReactElement {
    return (
        <section className="point">
        </section>
    );
}

export default Point;
